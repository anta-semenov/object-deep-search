{
  "name": "object-deep-search",
  "version": "0.0.7",
  "description": "Search through object properties recursively with a complex filter.",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "clean": "rimraf lib",
    "build": "babel src --out-dir lib --ignore test.js",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "search",
    "object",
    "deep",
    "filter",
    "find",
    "react-test-renderer"
  ],
  "author": "Anton Semenov",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.21.0",
    "babel-eslint": "^7.1.1",
    "babel-jest": "^18.0.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "eslint": "^3.12.2",
    "jest": "^18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/anta-semenov/object-deep-search.git"
  }
}
